using UnityEngine;

public class BallScriptsObserver : MonoBehaviour
{
    
    //this script is for simply enabling or disabling ball scripts for handling them at runtime
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
